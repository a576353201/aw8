location /index/ {
	if (!-e $request_filename){
		rewrite  ^(.*)$  /index.php?s=$1  last;   break;
	}
}
location /api/ {
	if (!-e $request_filename){
		rewrite  ^(.*)$  /index.php?s=$1  last;   break;
	}
}
location /api1/ {
	if (!-e $request_filename){
		rewrite  ^(.*)$  /index.php?s=$1  last;   break;
	}
}
location /addons/ {
	if (!-e $request_filename){
		rewrite  ^(.*)$  /index.php?s=$1  last;   break;
	}
}
location /wholesale/ {
       try_files $uri /wholesale/index.html;
}
location / {
       try_files $uri /index.html;
}

listen 443 ssl http2;
server_name iim.shopptw.com;
location / {
    proxy_pass http://47.242.172.149:8282; #代理到上面的地址去，格式：http://域名:端口号，
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "Upgrade";
    proxy_connect_timeout 5s; #配置点1
    proxy_read_timeout 60000s; #配置点2，如果没效，可以考虑这个时间配置长一点
    proxy_send_timeout 60000s; #配置点3
}

#SSL-START SSL相关配置，请勿删除或修改下一行带注释的404规则

php think wanlshop:chat start --daemon
php think wanlshop:order start --daemon


start php think wanlshop:chat --deploy register & start php think wanlshop:chat --deploy business &  start  php think wanlshop:chat --deploy gateway
ss


start php think antshop:chat --deploy register & start php think antshop:chat --deploy business &  start  php think antshop:chat --deploy gateway
ss


php think kefu start

zaIKFkYZSKzPhRqsjeVpgw==
以daemon（守护进程）方式启动
php think kefu start d

lAhSuDFwvvst8bFTmsDblQ==
停止
php think kefu stop

rWIfYFMyMVqD9EKTrxzCDQ==
重启
php think kefu restart

rlH4A/HBc19jDWhl6Q9PbQ==
平滑重启
php think kefu reload

foUFznAqmTkDWtoX+vsKDQ==
查看状态
php think kefu status


passthru,exec,system,putenv,chroot,chgrp,chown,shell_exec,popen,proc_open,pcntl_exec,ini_alter,ini_restore,dl,openlog,syslog,readlink,symlink,popepassthru,pcntl_alarm,pcntl_fork,pcntl_waitpid,pcntl_wait,pcntl_wifexited,pcntl_wifstopped,pcntl_wifsignaled,pcntl_wifcontinued,pcntl_wexitstatus,pcntl_wtermsig,pcntl_wstopsig,pcntl_signal,pcntl_signal_dispatch,pcntl_get_last_error,pcntl_strerror,pcntl_sigprocmask,pcntl_sigwaitinfo,pcntl_sigtimedwait,pcntl_exec,pcntl_getpriority,pcntl_setpriority,imap_open,apache_setenv